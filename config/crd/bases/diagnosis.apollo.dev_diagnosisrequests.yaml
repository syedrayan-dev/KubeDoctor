---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: diagnosisrequests.diagnosis.apollo.dev
spec:
  group: diagnosis.apollo.dev
  names:
    kind: DiagnosisRequest
    listKind: DiagnosisRequestList
    plural: diagnosisrequests
    singular: diagnosisrequest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DiagnosisRequest is the Schema for the diagnosisrequests API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DiagnosisRequestSpec defines the desired state of DiagnosisRequest.
            properties:
              policyRef:
                description: PolicyRef references the DiagnosisPolicy to use
                properties:
                  name:
                    description: Name of the DiagnosisPolicy
                    type: string
                  namespace:
                    description: Namespace of the DiagnosisPolicy
                    type: string
                required:
                - name
                - namespace
                type: object
              targetPod:
                description: TargetPod specifies the pod to diagnose
                properties:
                  name:
                    description: Name of the pod
                    type: string
                  namespace:
                    description: Namespace of the pod
                    type: string
                required:
                - name
                - namespace
                type: object
              triggerCondition:
                description: |-
                  TriggerCondition contains trigger information for automatic diagnosis
                  Required when Type is "Automatic"
                properties:
                  detectedAt:
                    description: DetectedAt is when the condition was detected
                    format: date-time
                    type: string
                  type:
                    description: Type of the condition (e.g., "CrashLoopBackOff",
                      "ImagePullBackOff")
                    type: string
                required:
                - detectedAt
                - type
                type: object
              type:
                description: Type indicates whether this is an automatic or manual
                  diagnosis request
                enum:
                - Automatic
                - Manual
                type: string
            required:
            - policyRef
            - targetPod
            - type
            type: object
          status:
            description: DiagnosisRequestStatus defines the observed state of DiagnosisRequest.
            properties:
              completionTime:
                description: CompletionTime is when the diagnosis completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              diagnosisReportRef:
                description: DiagnosisReportRef references the created DiagnosisReport
                properties:
                  name:
                    description: Name of the DiagnosisReport
                    type: string
                  namespace:
                    description: Namespace of the DiagnosisReport
                    type: string
                required:
                - name
                - namespace
                type: object
              error:
                description: Error contains error information if the request failed
                properties:
                  message:
                    description: Message is a human readable error message
                    type: string
                  reason:
                    description: Reason is a machine readable error reason
                    type: string
                  retryCount:
                    description: RetryCount is the number of retry attempts made
                    type: integer
                required:
                - message
                - reason
                type: object
              lastUpdateTime:
                description: LastUpdateTime is when the status was last updated
                format: date-time
                type: string
              message:
                description: Message contains a human-readable message about the current
                  state
                type: string
              phase:
                description: Phase represents the current phase of the diagnosis request
                enum:
                - Pending
                - InProgress
                - Completed
                - Failed
                type: string
              requestTime:
                description: RequestTime is when the request was created
                format: date-time
                type: string
              startTime:
                description: StartTime is when the diagnosis started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
